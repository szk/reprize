#ifndef MAKE_STRIP_HPP_
#define MAKE_STRIP_HPP_

namespace reprize
{
namespace vid
{
class MakeStrip
{
public:
    MakeStrip(void);
    virtual ~MakeStrip(void);

//     void gen_strips(std::vector<res::Vertex*>* vertex, res::Mesh* mesh);
//     void remap_indices(std::vector<res::Vertex*>* vertex_v, std::vector<res::Mesh*>* mesh_v);
protected:
};
}
}
#endif
